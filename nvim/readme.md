# plox-nvim

Personal neovim configuration. Still trying to focus on minimalism and using 
standard features only.

## Dependencies

Besides compiling neovim, will need to install the language servers that are
defined in the configuration.
